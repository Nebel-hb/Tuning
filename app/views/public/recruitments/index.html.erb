
<h2>募集一覧</h2>

<%= form_with url: recruitments_path, local: true, method: :get do |f|%>
   
  <%= f.label :title_cont, "タイトル" %>
  <%= f.text_field :title , placeholder: "search" %>

  <%= f.label :area_id_eq,  "地域"%>
  <%= f.collection_select :area, Area.all, :id, :area_name, prompt: "----", required: "required", class: 'page-title', placeholder: " "%>

  <%= f.label :start, "開始日時", class: 'control-label' %>
  <%= f.datetime_field :recruit_end_gteq, class: 'form-control' %>

  <%= f.label :end, "終了日時", class: 'control-label' %>
  <%= f.datetime_field :recruit_start_lteq, class: 'form-control' %>

  <%= f.label :start, "開始日時", class: 'control-label' %>
  <%= f.datetime_field :recruit_event_end_gteq, class: 'form-control' %>

  <%= f.label :end, "終了日時", class: 'control-label' %>
  <%= f.datetime_field :recruit_event_start_lteq, class: 'form-control' %>

<!--%= f.select :search_all, [['after today', 1], ['all', 2]], class: 'sample' %>-->

  <%= f.submit "search" %>
<% end %>


<% if current_user.role == "orchestra" %>

            <%= link_to new_recruitment_path do %>New<% end %>
 <% end %>
        <table class="table">
          <thead>
            <tr>
              <!--<th>image</th>-->
              <th>タイトル</th>
              <th>地域</th>
              <th>募集者</th>
              <th>recruit_introduction</th>
              <th>募集開始日</th>
              <th>募集終了日</th>
              <th>本番開始日</th>
              <th>本番終了日</th>
              <th colspan="3"></th>
            </tr>
          </thead>
    <!--      <form>-->
    <!--<div class="form-check">-->
    <!--    <input class="form-check-input" type="radio" name="q[event_tags_id_eq]" value="event" onclick="formSwitch()" checked>-->
    <!--    <label class="form-check-label"> 募集中</label>-->
    <!--</div>-->
    <!--<div class="form-check">-->
    <!--    <input class="form-check-input" type="radio" name="q[event_tags_id_eq]" value="tag" onclick="formSwitch()">-->
    <!--    <label class="form-check-label"> 過去の募集も表示</label>-->
    <!--</div>-->
</form>

<ul>
  <tbody>
          <%= @recruitments.each do |recruit| %>
   <tr>

          <td>
            <%= link_to recruitment_path(recruit.id) do %>
              <%= recruit.title %>
            <% end %>
          </td>
          <td>
            <%= recruit.area.area_name %>
          </td>
          <td>
            <%= recruit.user.name %>
          </td>
          <td>
            <%= truncate(recruit.recruit_introduction, length:15) %>
          </td>
          <td>
            <%= l recruit.recruit_start %>
          </td>
          <td>
            <%= l recruit.recruit_end %>
          </td>
          <td>
            <%= l recruit.recruit_event_start %>
          </td>
          <td>
            <%= l recruit.recruit_event_end %>
          </td>
        </tr>
       </div>
      </tbody>
      <% end %>

        </table>
    </div>
  </div>
