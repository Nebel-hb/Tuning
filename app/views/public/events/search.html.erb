<h2>投稿一覧</h2>
　<h3>タグ一覧</h3>
  <% @tag_list.each do |list| %>
    <span>
      <%= link_to list.tag_name, tag_events_path(event_tag_id: list.id) %>
      <%= "(#{list.events.count})" %>
    </span>
  <% end %>
  <br>

<%= "タグが ─ " %>
<strong><%= "#{@tag.tag_name}" %></strong>
  <%= " ─ の投稿一覧" %>
<br>

  <% @events.each do |event| %>
    <%= link_to event.title, event %>
  <% end %>

<%= link_to 'イベント一覧へ', events_path %>